//it is the configuration code for cdn tailwind

/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{html,js}"],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  var newWord=document.getElementById('appendix-element').attributes;
  console.log(newWord)